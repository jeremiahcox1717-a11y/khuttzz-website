# KHUTTZZ

Open the project folder and launch `index.html`.

- Home: `index.html`
- Booking: `book.html`

Need the whole folder (`styles.css`, `script.js`, both HTML files) so the black layout loads.
